# 授業カレンダー 仕様書（SPEC）

最終更新: 2026-07-21 / ステータス: 承認済み（Phase 1 完了）→ Phase 2/3 へ

---

## 確定した前提（Phase 1 で合意）

1. 対象は対象校舎のみ（`MANAERP__Location__c = "<SF_LOCATION>"`）。
2. 閲覧専用。Salesforceへ書き戻さない。講師は使わない（事務用）。
3. **生徒名は表示しない。** セルには講師名だけを出す。
4. 目的は「誰がどの時間に空いているかを一目で判断する」こと。授業が入っている時間帯に、その講師名が出ていればよい。
5. 認証は共通パスワード1つ（閲覧専用の簡易保護）。
6. フロント=Next.js（Vercel）、SF取得=Python バッチ（※後に Next.js からの直接取得へ変更。9章参照）。
7. 表示の主データは日付実体 `MANAERP__Lesson__c`（講師が入る）。`MANAERP__Lesson_Schedule__c` は期間（いつから空くか）の補助。

## 1. 目的（Objective）

対象校舎の授業を「講師 × 曜日 × 時間帯」の週テンプレートで一覧表示し、代講や新規授業の割当時に「その時間に空いている講師」を一目で判断できるようにする。各枠に「いつまで＝いつから空くか」を併記し、2か月先までの空きを把握する。

- 利用者: 対象校舎の事務。
- Source of Truth: Salesforce（読み取り専用）。
- 解決する不便: 代講・割当のたびにSFを見るのが面倒／SFに一覧ビューが無く見にくい。
- ユーザーストーリー:
  - 事務として、ある曜日・時間に授業が入っていない講師を一目で見つけたい（代講を素早く割り当てるため）。
  - 事務として、途中終了する枠が「何日から空くか」を知りたい（次の生徒を入れる計画のため）。

## 2. データソース（Salesforce）

接続: SOAPログインで sessionId を取り、REST API で SOQL を実行する（読み取り専用）。
接続先ホストは `SF_LOGIN_DOMAIN`、資格情報は `SF_USERNAME` / `SF_PASSWORD` / `SF_SECURITY_TOKEN`。

### 主データ: `MANAERP__Lesson__c`（授業の日付実体）
- 抽出条件: `MANAERP__Location__c = '<SF_LOCATION>'` かつ `MANAERP__Lesson_Date__c >= TODAY` かつ `<= NEXT_N_DAYS:62` かつ `MANAERP__Status__c = 'Published'`。
- 使うフィールド:
  - `MANAERP__Teacher__c`（講師名） … 表示する唯一の名前。
  - `MANAERP__Start_Date_Time__c` / `MANAERP__End_Date_Time__c`（datetime, UTC） … 時刻算出。
  - `MANAERP__Lesson_Date__c`（date）, `MANAERP__Day__c`（曜日）。
  - `MANAERP__Lesson_Schedule__c`（親Schedule参照） … 枠の同一性・継続期間の補助。
  - `Name` は取得するが**表示に使わない**（生徒名を含むため）。枠の同一性判定は `(講師, 曜日, 開始時刻)` を基本キーとし、必要時のみ Schedule Id を併用。
- 規模実測（2026-07-21, 対象校舎・今後62日・Published）: 1257件・講師30名分。

### 補助データ: `MANAERP__Lesson_Schedule__c`（授業予定 = 定期枠テンプレ）
- `MANAERP__End_Date_Time__c` が枠の終了日 → 「いつから空くか」の根拠。
- 対象校舎識別は `MANAERP__Account__r.Name = '<SF_LOCATION>'`。
- 注意: `MANAERP__Teacher__c` はほぼ空（講師は実体側）。

### 確定した扱いのルール
- タイムゾーン: SFはUTC保存。**JST = UTC + 9**（実測照合済み）。曜日・開始時刻はJST変換後に算出。
- 除外: `Status = 'Cancelled'`、`Name` が「未定」で始まる枠（講師未定）、`Name` に「当欠」を含む枠、
  および割当対象外の講師の枠（環境変数 `EXCLUDED_TEACHERS` に挙げた名前を `transform.excludedTeachers()` で除外）。
- 体験授業: **除外しない**。講師の時間は埋まるため、塗りつぶしたうえで別色（緑）にして区別する。
  判定は授業名に「体験」を含むかで行う。`MANAERP__Lesson_Type__c` の体験区分は実データで0件のため使えない。
- 講師名の正規化: 「山田太郎」と「山田 太郎」のような空白差を名寄せ（全角/半角スペース除去）。`Teacher` が空のLessonは表示から除外（生徒名も講師名も出す情報が無いため）。
- 生徒名・科目は取得しても保存・表示しない。

## 3. 表示形式と「空き」の定義

- レイアウト: **縦=講師名 / 横=時間（30分刻み）** のマトリクス。曜日はタブで切り替える。
  講師名が左端に揃うので「その講師が何時に空いているか」を横に追える。
  （当初は現行アプリと同じ 縦=時間/横=曜日 にしたが、名前が散らばって空きを追えないため変更した）
- 表示する曜日: 月〜土。**日曜は基本的に授業が無いため表示しない**。
- セルの状態: 授業が入っている時間は塗りつぶし、空いている時間は空白。
- 空き開始日: 同一グループのLessonの最終日を求め、途中で無くなる枠は色を変え、
  マウスオーバーで「MM/DD から空き」を表示。
- 集約キーは `(正規化講師名, JST曜日, JST開始時刻)`。

### 手動の印（〇×）

Salesforceには無い情報（「空いているが講師の都合で入れられない」）を、事務が手で書き足せるようにする。

- 空きセルをクリックすると 無印 → ×（入れない）→ 〇（入れる）→ 無印 と一巡する。
- 粒度は集約キーと同じ `(講師, 曜日, 時刻)` の週テンプレート。一度付ければ毎週その枠に出る。
- **Salesforceが優先**。その枠に授業が入れば印は表示しない（描画時に判定するだけで、保存値は消さない）。
- 全員で共有する（保存先は下記）。閲覧者を区別しないので、誰が付けたかは記録しない。

### 手で足す講師

授業が確定していない講師にも「その曜日なら入れるかもしれない」と印を付けたいので、表に行を足せる。

- 候補は在籍講師（`Contact`: RecordType=Staff / Working_Status が Available か研修中 /
  `Teacher_Type__c` = 通常講師）から、その曜日に既に出ている人と追加済みの人を除いたもの。
  名寄せは授業側と同じ空白除去。`EXCLUDED_TEACHERS` に挙げた講師は除く。
- 粒度は `(講師, 曜日)`。足した行は表の末尾に区切り線を挟んで並べる。
- **授業が1コマでも入れば、その講師は通常の段へ移る**（二重に出さない）。印はキーが
  `(講師, 曜日, 時刻)` なので行の位置に依らず残る。授業が無くなればまた足した行に戻る。

## 4. 技術スタック（Tech Stack）

- Next.js（App Router）+ TypeScript のみ。Vercel にデプロイする。
- Salesforce へはサーバー側から直接接続する（`lib/salesforce.ts`）。SOAPログインで sessionId を取り、
  REST API で SOQL を実行する（ページング対応・読み取り専用）。
- 資格情報: 本番は Vercel の環境変数、ローカルは `web/.env.local` を `lib/env.ts` が読む。
- 更新: 表示時に取得して10分キャッシュ（タグ `grid`）。画面の更新ボタンが Server Action で
  `updateTag` を呼び、次の描画で取り直す。日次バッチや外部Cronは使わない。
  （当初はPythonバッチが `grid.json` を生成する構成だったが、Vercel上でボタン更新できないため移行した）
- 認証: proxy（Next.js 16 で middleware から改名。ランタイムは Node.js）による共通パスワード
  （cookie にはハッシュを保存）。
- 手動の印の保存先: Vercel の Upstash Redis に Hash（キー `lesson-calendar:marks`）で持つ。
  REST を fetch で叩くだけなので npm の依存は増やさない。フィールド単位（HSET/HDEL）で
  更新するため、複数人が同時に触っても他の印を巻き戻さない。
  ローカル開発は環境変数が無いので `.data/marks.json`（gitignore済み）に書く。
  Vercel Blob を使わない理由: 無料枠では公開URLになり、講師名が認証なしで読めてしまう。

## 5. コマンド（Commands）

`web/` で実行する。

- 開発: `npm run dev`
- テスト: `npm test`（vitest）
- 型チェック: `npm run lint`（`tsc --noEmit`）
- ビルド: `npm run build`（dev を止めてから実行する。動かしたままだと `.next` が壊れる）

## 6. プロジェクト構成（Project Structure）

```
teacher-availability-calendar/
  SPEC.md / TASKS.md / README.md
  web/
    proxy.ts               共通パスワード認証
    lib/
      salesforce.ts        SOAPログイン + REST でSOQL実行（ページング対応）
      transform.ts         JST変換・名寄せ・除外・体験判定・集計・空き算出（純関数）
      data.ts              取得とキャッシュ（10分 / タグ grid）
      grid.ts              時間軸と講師行の組み立て（純関数）
      marks.ts             〇×の印のキー・切替・表示解決（純関数）
      marks-store.ts       印の保存（Upstash Redis / ローカルは .data/marks.json）
      env.ts               ローカルの .env.local 読み込み
      auth.ts              cookie用トークン
    app/
      page.tsx             画面（サーバーコンポーネント）
      WeekView.tsx         曜日タブ + 講師×時間マトリクス + 印のクリック
      RefreshButton.tsx    更新ボタン
      actions.ts           Server Action（updateTag / 印の保存）
      login/page.tsx       パスワード入力
    __tests__/             transform / grid / marks / marks-store / auth のユニットテスト
```

## 7. コードスタイル（Code Style）

- 変換は純関数に集約し、Salesforce I/O と分離する。テストは純関数側に寄せる。

```ts
/** 講師名の空白差を吸収する（実データで「山田太郎」と「山田 太郎」が分裂しているため）。 */
export function normalizeTeacher(name: string | null | undefined): string | null {
  if (!name) {
    return null;
  }
  return name.replace(/[\s　]/g, "") || null;
}
```

- 取得はサーバーコンポーネント / Server Action、描画はクライアントコンポーネント。

## 8. テスト戦略（Testing Strategy）

- 変換ロジック（TZ変換, 名寄せ, 除外, 体験判定, 集計, 空き開始日）と画面の組み立ては純関数化し `vitest` で検証する。
- バグ修正は再現テストを先に書く（CLAUDE.md準拠）。
- 代表ケース: UTC→JST変換、山田の2表記が1人に集約、途中終了枠の空き開始日、
  Teacher空・社員・未定・当欠の除外、体験フラグ、日曜の除外。

## 9. 境界（Boundaries）

- Always: SFは読み取りのみ／変換修正は再現テスト→`npm test`／`.env` の値は読まず環境変数として扱う。
- Ask first: 依存追加・抽出条件（拠点/期間/ステータス）変更・本番デプロイ（Vercel公開）。
- Never: 資格情報のコミット／SFの更新削除／生徒個人情報の保存・表示／認証なしでの講師名公開。

## 10. 成功基準（Success Criteria, testable）

1. 対象校舎・今後62日・Published の授業が講師×時間のマトリクスに欠落なく反映される。
2. セルには講師名のみ（生徒名・科目は出さない）。
3. 「山田太郎」と「山田 太郎」が1人に集約。
4. 途中終了枠に空き開始日が出て、SFの最終Lesson日と一致。
5. 時刻がJSTで正しい。
6. 共通パスワードを通さないと画面が見えない。
7. 更新ボタンでSalesforceから取り直せる。
8. 空きセルのクリックで 無印 → × → 〇 → 無印 と切り替わり、保存されて他の人にも見える。
9. 印を付けた枠に授業が入ると、印は表示されなくなる（Salesforceが優先）。

## 11. 未解決事項（Open Questions）

1. 「空き」の粒度: 個別授業（定員1）は「その枠にLessonが無い＝空き」で運用中。グループ授業の定員割れは将来検討。
2. キャッシュ時間: 現在は10分。更新ボタンがあるのでもっと長くても良いか。
3. （解決済み）その曜日に授業が無い講師にも印を付けたい → 在籍講師から手で行を足せるようにした。
4. 印にゴミが溜まる件: 講師が辞めても保存値は残る（表示されないだけ）。件数が小さいので放置。
   消す手段が必要になったら一覧と削除の画面を足す。足した講師のフラグも同様。
5. （解決済み）本番公開: Vercel で稼働中。
6. （解決済み）現行アプリの「授業あり/毎週希望/単発代講可」フィルタ相当は、手動の〇×で代替した。

## 12. 機能追加: 生徒マッチング（2026-07-22 / Phase 1・レビュー待ち）

### 12.1 目的

生徒の希望（科目・時間帯・文理・大学）を入力すると、条件に合う講師を候補として出す。
今は「空いているか」しか分からないので、事務が科目の可否を別途SFで確認している。その往復をなくす。

- 利用者: 対象校舎の事務（既存画面と同じ、認証の内側）。
- ユーザーストーリー: 事務として、新規生徒の希望科目と希望時間帯を入れたら、
  その科目を教えられて、その時間が空いている講師を一覧で見たい。

### 12.2 前提（合意済み）

1. 生徒の希望は**画面で手入力**する。Salesforceの生徒レコードからは引かない。
2. **生徒名は入力欄も表示も作らない**（3章までの方針を継続）。
3. 絞り込みに使うのは 指導可能科目 / 希望時間帯 / 文理 / 大学名 の4つ、および手動の×印。
4. 希望時間帯は**複数を並べて指定**し、どれか1つでも満たせば候補にする。
5. 結果は**候補リストと週グリッドの絞り込みの両方**で見せる。
6. 「空き」の判定は既存と同じ（その枠にLessonが無い＝空き）。

### 12.3 追加で取るSalesforceデータ

いずれも読み取りのみ。実測値は 2026-07-22 に確認したもの。

指導可能科目 `MANAERP__Eligible_Subject__c`（Contactの子。1科目1レコード）:

```sql
SELECT MANAERP__Contact__r.Name, MANAERP__Subject__r.Name
FROM MANAERP__Eligible_Subject__c
WHERE MANAERP__Contact__r.RecordType.Name = 'Staff'
  AND MANAERP__Contact__r.MANAERP__Working_Status__c IN ('Available','研修中')
  AND MANAERP__Contact__r.Teacher_Type__c = '通常講師'
```

- 実測 184件。うち9件は割当対象外の講師の分で、`EXCLUDED_TEACHERS` により捨てる。
- 重複レコードが1件ある（同じ講師に同じ科目が2件）。**組み立て時に重複を除く**。
- 科目マスタ `MANAERP__Subject_Master__c` は36件あるが、**社員を除いた在籍29名が実際に持つのは16種**
  （高校英語27／高校国語 現代文19・古文18・漢文18／高校数学 ⅠA18・ⅡB18・Ⅲ11／高校世界史10／
  高校化学10／高校物理7／高校日本史6／高校地理4／高校生物4／高校政治経済2／高校化学基礎1／高校生物基礎1）。
  選択肢は「実際に誰かが持っている科目」から作る（誰も教えられない科目を選ばせない）。
- 科目0件の講師が1名いる（清水蓮。担当が事務のため）。科目を1つでも指定すると候補に出ない＝正しい挙動。
- **中学科目は選択肢に出ない**。`中学数学` `中学英語` を持つのは社員の鈴木一郎だけで、
  社員は授業の割当対象外だから。**これは仕様として許容する**（2026-07-22 に確認）。
  高校の英語・数学ができれば中学生も見られるため、中学生は高校科目で探し、
  コマ長だけ90分・120分を選べばよい。

在籍講師 `Contact`（既存 `ROSTER_SOQL` に2列足す）:

- `humanities_sciences_cd_t__c`（文理）… 社員を除く29名**全員に入っている**（文系19／理系10）。
  選択肢が 1/2/3 の `Humanities_Science_Code__c` は**別物なので使わない**。
- `University_Name__c`（大学名）… 社員を除くと15種。最多は早稲田大学9名で、
  残りは1〜2名ずつ。**大学で絞ると候補がほぼ0になる**ので、絞り込みではなく並べ替えに使う。
- `Faculty_Name__c`（学部名）… 医学部志望の生徒に医学部の講師を当てるために使う。
  当初はほぼ空だったので、別途スクリプトで対象講師に入れてある。

名寄せの整合は確認済み: 授業に出る講師26名は全員が在籍名簿29名（社員2名を除いた数）に含まれ、
名簿に無いのに授業に出る講師は0名。よってプロフィールは名簿側から作れば取りこぼさない。

### 12.4 入力する希望条件

```ts
export type TimeWish = { weekday: string; from: string; to: string };

/** 1コマの長さ。高校生は全員60分、中学生は90分と120分の2通り。 */
export const LESSON_MINUTES = [60, 90, 120] as const;

export type Wish = {
  subjects: string[];                  // 空なら科目で絞らない。複数選択時は全部教えられる講師（AND）
  bunri: "文系" | "理系" | null;       // 絞り込み
  preference: string | null;           // 志望先（大学名または学部名）。並べ替えにだけ使う
  times: TimeWish[];                   // 空なら時間で絞らない
  lessonMinutes: number;               // 既定60（高校生）
  excludeNg: boolean;                  // ×印の枠を空きに数えない（既定true）
};
```

科目は**常にAND**（選んだ科目を全部教えられる講師）。2科目を同じ講師に任せる必要がある場合があるため。

コマ長の選択肢は用途をラベルに出す（`60分（高校生）` / `90分（中学生）` / `120分（中学生）`）。
科目の選択肢は区分で絞らない。中学生に高校科目の講師を当てる運用があり得るため、18種すべて出す。

### 12.5 マッチの判定

空きスロットの定義（30分刻み、既存の `STEP_MINUTES` と同じ）。**その曜日に授業があるかで扱いを変える**:

- その曜日に授業が1コマでもある講師 … 授業のセルが無い時刻が空き。
  `excludeNg` が有効なら ×印の枠は空きに数えない。
- その曜日に授業が無く、手で足しただけの講師（デビュー前など） …
  **〇印を付けた枠だけが空き**。表に出ていないだけで来られないとは限らないが、
  空いている確証も無いため、事務が〇を付けて初めて候補にする。
  （この扱いでは×は関係しないので `excludeNg` の影響を受けない）

希望を満たす判定:

- 希望1件 `{曜日, from, to}` の範囲内で、**連続した空きスロットが `lessonMinutes` 以上**あれば満たす。
  「範囲を丸ごと空けていること」は厳しすぎ、「1スロットでも空いていること」は緩すぎるため、
  実際に1コマ入るかで見る。
- 希望が複数あるときは**どれか1つでも満たせば候補**。満たした希望と、その中の空き時間帯を結果に持つ。

講師が候補になる条件（指定のあるものだけ効く）:

- 科目: 選んだ科目を**すべて**持つ。
- 文理: 指定があれば一致。
- 時間: 希望が1件以上あるとき、上記のどれかを満たす。
- **志望先は候補を減らさない**（下記の並べ替えにだけ使う）。

並び順:

1. 満たした枠に〇印（入れると確認済み）がある講師
2. 志望先と一致する講師（**大学名または学部名**のどちらかが一致すれば良い）
3. 満たした希望の数が多い順
4. 名前順

〇印を志望先より先に置くのは、〇印が「入れると確認済み」という実務上の確定情報なのに対し、
志望先は好みだから。「近い大学」は難易度のデータが無いので**名前の一致だけ**で判定する。

志望先のプルダウンは学部と大学を分けて出す（「医学部」を選べば医学部の講師が上に来る）。
学部はSFに入っていても探す軸として意味があるとは限らないので、
`profile.SEARCHABLE_FACULTIES` に書いたものだけを候補に出す（現在は医学部のみ。
高橋美咲の建築学部はSFにあるが出さない）。

### 12.6 画面

既存の週グリッドの上に条件入力の帯を置き、その下に候補リスト、さらに下に既存のグリッドを残す。

- 条件が空のときは今までと完全に同じ画面（候補リストも絞り込みも出ない）。
- 絞り込むと、既定では**該当講師の行だけ**を残す。「全員表示」で元に戻せる。
  条件外の講師も見たい場合があるため、消すのではなく切り替えにする。
- 候補リストに出すのは 講師名 / 大学・文理 / 満たした希望と空き時間帯 / 〇印の有無。**生徒名は無い**。
  志望校と同じ大学の講師には目印を付ける。
- 候補0名のときは、どの条件で落ちたか（科目で何名、時間で何名）を出す。
- 手で足した講師（`extra`）の行も対象にする。授業が無いので全スロットが空き扱いになる。
- 条件は画面の状態としてのみ持ち、保存しない。

希望の時間帯は**曜日を複数選んでまとめて足す**（`match.addTimeWishes`）。
「平日の18〜22時」が1回の操作で入るようにするため。まとまりの近道として
`平日` `土` `全部` を置く（押すと選択を置き換える）。足したあとは1件ずつの
`TimeWish` なので、曜日も時刻も行ごとに直せるし消せる。
同じ曜日・同じ時刻の希望は二重に足さない（押し間違いで行が増えないように）。

### 12.7 追加・変更するファイル

```
web/lib/profile.ts        SFレコード → 講師プロフィール（純関数）
web/lib/match.ts          希望条件 → 候補（純関数。テストの主対象）
web/lib/data.ts           SOQL追加、GridData に profiles を足す（変更）
web/lib/grid.ts           行の絞り込みを受ける（変更）
web/app/MatchPanel.tsx    条件入力と候補リスト（クライアント）
web/app/WeekView.tsx      絞り込み結果の反映（変更）
web/__tests__/profile.test.ts / match.test.ts
```

### 12.8 テスト戦略

判定は全て純関数に寄せ、`vitest` で検証する。代表ケース:

- 連続空きが `lessonMinutes` に足りない枠は候補にならない（60分空き / 90分必要）。
- 同じ空きでも 60分なら候補、120分なら候補外になる。
- ×印を挟むと連続が切れる（`excludeNg` の有無で結果が変わる）。
- 複数希望のどれか1つを満たせば候補になる。
- 2科目を指定すると、両方持つ講師だけが残る（片方だけの講師は落ちる）。
- 科目0件の講師は科目指定時に候補から外れる。
- 文理が未指定なら効かない（空の値で候補を減らさない）。
- **志望校を指定しても候補の人数は変わらず、並び順だけが変わる**。
- 既存テスト78件が通り続ける。

### 12.9 境界

- Always: SFは読み取りのみ／判定の修正は再現テストを先に書く／既存の〇×と講師追加を壊さない。
- Ask first: 依存追加・SOQLの抽出条件変更・本番デプロイ。
- Never: 生徒名の入力欄や表示を作る／プロフィールを認証の外に出す。

### 12.10 成功基準（検証可能）

1. 科目を複数選ぶと、そのすべての `Eligible_Subject` を持つ講師だけが候補になる。
2. 希望時間帯を複数入れると、どれか1つで1コマ分の連続空きがある講師だけが候補になる。
3. コマ長を60→120分に変えると、短い空きしかない講師が候補から外れる。
4. ×印の枠は空きに数えない（`excludeNg` を外すと数える）。
5. 文理は未指定なら候補を減らさない。志望先は指定しても人数を変えず、並び順だけ変える。
   志望先に「医学部」を選ぶと、学部が医学部の講師が上に来る。
6. 候補0名のとき、どの条件で落ちたかが画面に出る。
7. 生徒名の入力欄が無く、画面のどこにも生徒名が出ない。
8. 条件が空のとき、既存の画面と挙動が変わらない（既存テスト78件が通る）。

### 12.11 レビューで確定した点（2026-07-22）

1. 複数科目は**常にAND**。2科目を同じ講師が担当しなければならない場合があるため、OR切替は作らない。
2. コマ長は **60分（高校生・既定）/ 90分（中学生）/ 120分（中学生）**。高校生は全員60分。
3. 大学は**絞り込みではなく並べ替え**。志望校と同じ大学の講師を上に出す。
   絞り込みにすると17種で1〜2名ずつのため候補がほぼ0になる。
4. 絞り込み中は**該当講師の行だけ**表示する（薄く残さない）。
5. 中学科目・基礎科目も選択肢に出す。中学生の生徒がいるため。
